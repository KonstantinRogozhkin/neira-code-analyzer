---
description: 
globs: 
alwaysApply: true
---
# neira-code-analyzer

MCP сервер для продвинутого анализа кодовых баз с профессиональными шаблонами анализа и мощными инструментами оптимизации фильтров.

## 🎯 Возможности

**4 мощных инструмента для работы с кодом:**

1. **🤖 `get_analyze`** - Автоматический AI анализ кода (⭐ **Флагманская функция**)
   - Полностью автоматизированный анализ кода с Neira
   - Интеллектуальная оптимизация фильтров для соблюдения лимитов токенов
   - Многоступенчатый процесс: анализ структуры → оптимизация → извлечение → AI-анализ
   - Сохранение результатов в версионированную структуру файлов

2. **🔍 `analyze_filters`** - Анализ и оптимизация фильтров файлов
   - 💡 **Принцип:** *"Provide as little context as possible, but as much as necessary"*
   - Агрессивное исключение ненужных файлов
   - Статистика по типам файлов и размерам токенов
   - Готовые оптимизированные конфигурации

3. **📄 `get_context`** - Генерация контекста для ИИ анализа
   - 7 профессиональных шаблонов (code-review, security-audit, документация и др.)
   - Гибкая фильтрация с glob-паттернами
   - Подсчет токенов для разных LLM

4. **📋 `get_templates`** - Просмотр доступных шаблонов анализа

## О проекте

neira-code-analyzer использует высокопроизводительную Rust библиотеку [code2prompt-rs](mdc:https:/github.com/mufeedvh/code2prompt) для анализа кодовых баз и создания структурированных резюме, оптимизированных для потребления ИИ.

## ⚙️ Установка

neira-code-analyzer это **MCP (Model Context Protocol) сервер**, который интегрируется с LLM клиентами через стандарт MCP.

### Установка зависимостей

```bash
# Клонируйте репозиторий
git clone https://github.com/odancona/neira-code-analyzer.git
cd neira-code-analyzer

# Установите зависимости (поддерживается uv и rye)
uv sync
# или
rye sync
```

## 🚀 Быстрый старт

### 1. Подключение MCP сервера

Добавьте сервер в конфигурацию вашего MCP клиента:

```json
{
  "servers": {
    "neira-code-analyzer": {
      "command": "uv",
      "args": ["run", "python", "-m", "src.neira_code_analyzer.main"],
      "cwd": "/path/to/neira-code-analyzer"
    }
  }
}
```

### 2. Пример использования analyze_filters

```json
{
  "path": "/Users/username/Projects/my-project",
  "include_patterns": ["*.py"],
  "exclude_patterns": ["tests/**", "__pycache__/**"]
}
```

### 3. Пример использования get_analyze (⭐ Рекомендуется)

```json
{
  "path": "/Users/username/Projects/my-project",
  "template_name": "code-review",
  "include_patterns": ["*.py", "*.js"],
  "exclude_patterns": ["tests/**", "node_modules/**"],
  "max_tokens": 500000,
  "google_ai_model": "gemini-2.5-pro-preview-06-05"
}
```

### 4. Пример использования get_context

```json
{
  "path": "/Users/username/Projects/my-project", 
  "template_name": "code-review",
  "include_patterns": ["*.py", "*.md"],
  "exclude_patterns": ["tests/**", "__pycache__/**", ".git/**"]
}
```

📖 **Подробная документация:** См. [USAGE.md](mdc:USAGE.md)

## License

MIT License - See LICENSE file for details.

## Development

### Тестирование

Для тестирования MCP сервера используйте MCP Inspector:

```bash
npx @modelcontextprotocol/inspector uv run python -m src.neira_code_analyzer.main
```

### Отладка

Для отладки запустите сервер напрямую:

```bash
uv run python -m src.neira_code_analyzer.main
```
